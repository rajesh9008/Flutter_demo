class PodCast {
  PodCast(this.name, this.imageUrl, this.authorImageUrl, this.author, this.desc,
      this.followerCount);

  final String name, imageUrl, authorImageUrl, author, desc, followerCount;
}
